# Local development setup

Use this folder when cloning the repo and running MediHub frontend on your machine.

| Document | Purpose |
|----------|---------|
| [local-environment.md](./local-environment.md) | Env files, dev proxy, local URLs, and what needs the backend |

**Production:** [docs/production/](../production/README.md)

**Templates in git (copy these, do not edit):**

| Template | Copy to (gitignored) |
|----------|-------------------|
| [`.env.example`](../../.env.example) | `.env` or `.env.local` |
| [`.env.development.example`](../../.env.development.example) | `.env.development` |
| [`.env.production.example`](../../.env.production.example) | `.env.production` (build/deploy only) |
